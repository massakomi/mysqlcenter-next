/** @type {import('next').NextConfig} */
const nextConfig = {
  /*async redirects() {
    return [
      {
        source: '/dashboard/settings',
        destination: '/about',
        permanent: true,
      },
    ]
  },*/
};

export default nextConfig;

